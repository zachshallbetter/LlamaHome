"""Constants used across the project."""

from typing import List

# Supported models for the project
SUPPORTED_MODELS: List[str] = [
    "model_a",
    "model_b",
    "model_c"
]

# Environment variables used in the project
ENV_VARS: List[str] = [
    "ENV_VAR_1",
    "ENV_VAR_2",
    "ENV_VAR_3"
]
